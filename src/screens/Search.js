import { SafeAreaView, Text } from "react-native"

export const Search = () => {
    return (
        <SafeAreaView>
            <Text
                style={{
                     color: '#000', fontSize: 20 }}>
                    Search</Text>
        </SafeAreaView>

    )
}