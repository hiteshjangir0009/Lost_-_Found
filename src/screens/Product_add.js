import { SafeAreaView, Text } from "react-native"

export const Product_add = () => {
    return (
        <SafeAreaView>
            <Text
                style={{ color: 'white', fontSize: 20 }}>Add</Text>
        </SafeAreaView>

    )
}