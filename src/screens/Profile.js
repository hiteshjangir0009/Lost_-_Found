import { SafeAreaView, Text } from "react-native"

export const Profile = () => {
    return (
        <SafeAreaView>
            <Text
                style={{ color: 'white', fontSize: 20 }}>Profile</Text>
        </SafeAreaView>

    )
}