import { SafeAreaView, Text } from "react-native"

export const Home = () => {
    return (
        <SafeAreaView>
            <Text
            style={{color:'white',fontSize:20}}>home</Text>
        </SafeAreaView>

    )
}